{% extends 'admin.html.twig' %}

{% block body %}
    <h1>Proposition_fournisseur creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('proposition_fournisseurs_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
