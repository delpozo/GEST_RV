{% extends 'base.html.twig' %}

{% block body %}
    <h1>Devise</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ devise.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ devise.nom }}</td>
            </tr>
            <tr>
                <th>Symbole</th>
                <td>{{ devise.symbole }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('devise_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('devise_edit', { 'id': devise.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
